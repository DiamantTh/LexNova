<script lang="ts">
  import type { BootstrapData } from '../bootstrap'; import AppShell from '../components/AppShell.svelte'; import NoticeList from '../components/NoticeList.svelte'; import { flag, record, strings, text } from '../lib/data';
  let { data }: { data: BootstrapData } = $props(); const csrf = $derived(text(data.csrfToken)); const fail2ban = $derived(record(data.fail2ban));
</script>
<AppShell title="Instanzsicherheit" area="admin" csrfToken={csrf}><NoticeList errors={strings(data.errors)} messages={strings(data.messages)} /><nav class="subnav"><a href="/admin">Übersicht</a><a class="active" href="/admin/security">Sicherheit</a><a href="/admin/system">System</a></nav><section class="card preset-filled-surface-100-900 mt-6 p-6"><h2 class="h2">Fail2ban-Signallog</h2><div class="mt-4 flex flex-wrap gap-3"><span class:status-good={flag(fail2ban.enabled)} class:status-warn={!flag(fail2ban.enabled)} class="status-pill">{flag(fail2ban.enabled) ? 'aktiv' : 'inaktiv'}</span><span class:status-good={flag(fail2ban.writable)} class:status-bad={!flag(fail2ban.writable)} class="status-pill">{flag(fail2ban.writable) ? 'schreibbar' : 'nicht schreibbar'}</span></div><p class="mt-4 opacity-75">Quelle: {text(fail2ban.source) === 'database' ? 'Datenbank' : 'config.toml'} · Pfad: <code>{text(fail2ban.path)}</code></p><p class="mt-2 text-sm opacity-70">Das Signallog enthält nur Zeitstempel, Kennung und IP – keine Benutzernamen oder Credentials.</p><form method="post" action="/admin/security/fail2ban" class="mt-6 grid max-w-xl gap-4"><input type="hidden" name="__csrf" value={csrf}><label class="label"><span>Aktivierung</span><select class="select" name="mode"><option value="config" selected={text(fail2ban.source) === 'config'}>config.toml verwenden</option><option value="enabled" selected={text(fail2ban.source) === 'database' && flag(fail2ban.enabled)}>In Datenbank aktivieren</option><option value="disabled" selected={text(fail2ban.source) === 'database' && !flag(fail2ban.enabled)}>In Datenbank deaktivieren</option></select></label><button class="btn preset-filled-primary-500" type="submit">Einstellung speichern</button></form></section></AppShell>
