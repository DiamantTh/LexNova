# Verifikationsbericht Systeminformationen 2026-08-14

## Bezug

- Geprüfter Funktionscommit: `34867ba`
- Zeitpunkt des Funktionscommits: 2026-08-14 02:51:51 CEST (UTC+02:00)
- Ursprünglicher Berichtscommit: `adaa6e4`
- Ursprünglich erstellt: 2026-08-14 03:12:29 CEST (UTC+02:00)
- Branch: `master`
- Lokale PHP-Laufzeit: `8.5.9`

## Ergebnis

| Prüfung | Ergebnis |
|---|---|
| PHPStan Level 6, sequentiell | keine Fehler |
| PHP-CS-Fixer, sequentieller Dry-Run | keine Abweichungen |
| `composer test` | alle lokal ausführbaren Tests erfolgreich |
| Twig-Rendering von `/admin/system` | erfolgreich, 14.086 Byte HTML |
| `git diff --check` | keine Whitespace-Fehler |

Der TOTP-Verschlüsselungstest wurde auf der lokalen Laufzeit mangels Sodium
übersprungen. Das ist eine Eigenschaft der lokalen Laufzeit und keine
Einschränkung der Zielinstallation; `ext-sodium` bleibt eine deklarierte
Produktanforderung.

## Geprüfter Umfang

- Die Route `/admin/system` bleibt durch die Admin-Authentifizierung geschützt.
- Requestbezogene Webserverdaten werden vom PSR-7-Request übernommen.
- Host/OS, Kernel, Architektur, Hostname, Serverzeit, Load Average und
  Speicherplatz werden ohne Shell-Aufrufe ermittelt.
- Webserver, Protokoll, Gateway/SAPI, DocumentRoot und temporäres Verzeichnis
  werden dargestellt.
- PHP-Version, Zend Engine, geladene `php.ini`, Zeitzone, Speicher-, Laufzeit-,
  Eingabe- und Uploadlimits, OPcache, Fehleranzeige und PDO-Treiber werden
  dargestellt.
- Datenbankserver- und PDO-Clientversion werden unabhängig und fehlertolerant
  ermittelt.
- Wichtige Composer-Komponenten werden mit installierter Version angezeigt.
- PhpRedis, Relay und Predis werden als alternative Protokollclients separat
  geprüft. Ein nicht installiertes `ext-redis` ist kein Pflichtfehler, solange
  der externe Cache nicht konfiguriert ist oder ein anderer Client bereitsteht.
- Valkey bleibt das bevorzugte Serverprodukt; Redis wird nur als kompatibel
  ausgewiesen.
- Keine DB-, Cache- oder App-Secrets gelangen in die Systeminformationen.
